{
  "name": "borchgomez-sanity-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "seed:hero": "set -a; [ -f .env ] && . ./.env; set +a; sanity dataset import seed/hero.ndjson --dataset \"${SANITY_DATASET:-$SANITY_STUDIO_DATASET}\" --replace",
    "seed:purpose": "set -a; [ -f .env ] && . ./.env; set +a; sanity dataset import seed/purpose.ndjson --dataset \"${SANITY_DATASET:-$SANITY_STUDIO_DATASET}\" --replace",
    "seed:plans": "set -a; [ -f .env ] && . ./.env; set +a; sanity dataset import seed/plans.ndjson --dataset \"${SANITY_DATASET:-$SANITY_STUDIO_DATASET}\" --replace",
    "seed:about": "sanity exec scripts/seed-about.mjs --with-user-token",
    "seed:contact": "set -a; [ -f .env ] && . ./.env; set +a; sanity dataset import seed/contact.ndjson --dataset \"${SANITY_DATASET:-$SANITY_STUDIO_DATASET}\" --replace",
    "seed:portfolio": "sanity exec scripts/seed-portfolio.mjs --with-user-token",
    "seed:services": "sanity exec scripts/seed-services.mjs --with-user-token"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "sanity": "6.4.0",
    "styled-components": "6.5.3"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
